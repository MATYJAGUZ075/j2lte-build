# los20-build-full.yml — BUILD COMPLETO de LineageOS 20 para j2lte
#
# FIX-018 (2026-08-23): separado del lunch-check (que permanece intacto).
# FIX-028: kernel genera zImage-dtb con DTB incorporado.
# FIX-029: adaptación Clang del kernel 3.10.
# FIX-030 (2026-08-29): sincronizado con las optimizaciones probadas en
#   los20-build-stages.yml — symlinks de compat (libncurses/libtinfo/libc++),
#   WITH_DEXPREOPT_BOOT_IMG_AND_SYSTEM_SERVER_ONLY=true, diagnóstico de
#   almacenamiento, stats de ccache y medición de out/ antes y después del
#   build. No se agrega selector de stage (systemimage/boot/recovery): este
#   workflow sigue siendo específicamente para mka bacon.
# FIX-031 (2026-09-12): sincronizado el resto de las podas post-sync que
#   ya estaban probadas en los20-build-stages.yml (rust linux-x86 versiones
#   inactivas, module_sdk/art snapshot @3, runtime/mainline, Ronda 2, Tier 1
#   SAFE, autotest) — antes solo tenía darwin-x86 y android-emulator/
#   remoteexecution-client. El GUARD de espacio (FIX-014) se movió a DESPUÉS
#   de todas las podas (igual que en stages), no a la mitad. Se agregó
#   también el monitor de disco en vivo (muestreo normal/denso, dmesg probe,
#   last_gasp) y el rescate de telemetría ante fallo/ENOSPC, adaptados de
#   stages al target único "bacon" (sin loop de $STAGE/$EXTRA, ya que este
#   workflow no separa por etapas).
# FIX-032 (2026-09-13): sincronizados 3 agregados nuevos de
#   los20-build-stages.yml: env SKIP_ABI_CHECKS=true (salta ABI dumps
#   .sdump/.lsdump, leído por Soong vía IsEnvTrue en cc/sabi.go); step de
#   limpieza controlada de Swift preinstalado + toolchains extra (miniconda,
#   pipx, az, microsoft) justo después de liberar espacio del runner; y
#   step de diagnóstico de almacenamiento fuera de out/ (tmp, caches, opt,
#   usr/share, var/log) antes del build, condicionado a mode=='full-bacon'
#   igual que el resto de la sección de build de este workflow.
# FIX-033 (2026-09-17): ccache ELIMINADO por completo, igual que en
#   los20-build-stages.yml (run de esa etapa demostró que se puede recuperar
#   ~1G de runner y simplificar el step de dependencias sin restore/save de
#   actions/cache). NO re-agregar USE_CCACHE, CCACHE_EXEC, CCACHE_DIR,
#   restore/save de actions/cache, instalación del binario, ni stats de
#   ccache sin autorización explícita. Se sincronizaron también las 4 podas
#   post-sync que faltaban de stages: limpieza off-out (kotlinc/gradle/
#   runner-cache/actionarchivecache no usadas por este workflow), sdk/32,
#   extras profiling (system/extras/ioshark) y Darwin arch remnants (jdk11/
#   jdk8/build-tools/extract-tools/misc darwin-x86/darwin-arm64). Se agregó
#   también el step de diagnóstico "Inspección read-only de runner-cache y
#   Gradle" antes del build, igual que en stages. mka bacon sigue siendo el
#   único target de este workflow: no se agrega selector de stage ni la
#   lógica de systemimage/bootimage/recoveryimage de los20-build-stages.yml.
# FIX-034 (2026-09-17): revertido — el mecanismo de emergency checkpoint
#   (id: build + background: true en el step de mka, más un step separado
#   con wait: [build]) NO es sintaxis válida de GitHub Actions: un step solo
#   admite run/shell/uses/with/working-directory, y los steps de un job
#   siempre corren en secuencia — no existe forma nativa de que un step siga
#   en paralelo mientras corren los siguientes. Esto rompió la validación
#   del workflow (run 35294355736, línea 1015: "Unexpected value 'wait'").
#   El mismo patrón está presente en los20-build-stages.yml (steps "Build
#   targets"/"Guardia emergency checkpoint"/"Wait build") y fallaría igual
#   si se corre ahí — recomendado aplicar el mismo revert en ese workflow.
#   Se mantiene SOLO el flag --skip-soong-tests en el comando mka (eso sí es
#   válido, es un argumento del binario, no sintaxis de Actions). El step de
#   build vuelve a ser secuencial (sin id/background); el monitor de disco
#   interno sigue usando "&" dentro del propio script bash del step (eso es
#   backgrounding de procesos del shell, no de steps de Actions, y sí
#   funciona). Se eliminaron los steps "Guardia emergency checkpoint",
#   "Upload emergency checkpoint" y "Wait build". El "Rescate de telemetría"
#   (if: always(), ya existente) sigue siendo la red de seguridad real:
#   recupera stage/bacon.log y disk-monitor.log si el build falla o el job
#   es cancelado, que es lo máximo que Actions permite sin runners
#   self-hosted con lógica custom de reporte.
# FIX-035 (2026-09-21): portadas las optimizaciones generales de
#   los20-build-stages.yml que faltaban aquí, sin cambiar la naturaleza
#   `mka bacon` de este workflow: 8 podas post-sync tardías (sdk-tools-lib,
#   metalava, appcompat, chicas-R1F, miscfw, webview-noarm,
#   contents+sysroots, tanda2) + filtro .repo opción A (conserva
#   commits/tags/trees, elimina solo blobs, con guards G1-G5).
#   Deliberadamente NO portado: selector de stage, systemimage-guard,
#   bootimage/recoveryimage, EXTRA, limpieza tardía symbols (sintaxis
#   background:/wait: inválida en Actions + riesgo en zip OTA),
#   PRODUCT_MINIMIZE_JAVA_DEBUG_INFO (altera artefacto full),
#   WITH_DEXPREOPT=false (bacon usa BOOT_IMG_AND_SYSTEM_SERVER_ONLY).
#
# IMPORTANTE / FIX-014 (INFRASTRUCTURE-BLOCKED):
#   El runner hospedado ubuntu-24.04 (~145G disco, 4 vCPU, 16G RAM) NO alcanza
#   para un bacon completo: tras repo sync quedan ~17G libres y out/ necesita
#   50–90G adicionales. Por eso este workflow tiene un GUARD de disco que
#   aborta temprano si el espacio libre post-sync es menor al mínimo exigido.
#
#   Para ejecutar un build real hace falta un runner con recursos suficientes
#   (recomendado: self-hosted con >=16 vCPU, >=64G RAM, >=400G SSD libres).
#   Uso futuro con self-hosted:
#     gh workflow run los20-build-full.yml -f runner_label=<label> -f mode=full-bacon

name: LOS20 build full (guarded)

on:
  workflow_dispatch:
    inputs:
      runner_label:
        description: 'runs-on label (ubuntu-24.04 | self-hosted label)'
        required: false
        default: 'ubuntu-24.04'
      mode:
        description: 'modo del run'
        required: false
        default: 'lunch-only'
        type: choice
        options:
          - lunch-only
          - full-bacon
      min_free_gb:
        description: 'GB libres mínimos post-sync para permitir bacon'
        required: false
        default: '70'
      telemetry_free_gb:
        description: 'GB libres que activan telemetría densa de diagnóstico (solo lectura)'
        required: false
        default: '25'
      kernel_toolchain:
        description: 'Toolchain del kernel (experimento A/B): clang (CI actual) o gcc (TARGET_KERNEL_CLANG_COMPILE=false)'
        required: false
        default: 'clang'
        type: choice
        options:
          - clang
          - gcc

concurrency:
  group: los20-full-build
  cancel-in-progress: false

jobs:
  build:
    runs-on: ${{ github.event.inputs.runner_label || 'ubuntu-24.04' }}
    timeout-minutes: 350

    env:
      # ccache ELIMINADO por completo (ver FIX-033). NO re-agregar
      # USE_CCACHE, CCACHE_EXEC, CCACHE_DIR, restore/save de actions/cache,
      # instalación del binario, ni stats/mediciones de ccache sin
      # autorización explícita.
      # Salta la generación de ABI dumps (.sdump/.lsdump) que bacon no
      # necesita (solo los usa el goal debug findlsdumps). Reduce el pico de
      # out/soong. Leído por Soong vía IsEnvTrue en cc/sabi.go.
      SKIP_ABI_CHECKS: "true"
      TELEM_FREE_GB: ${{ github.event.inputs.telemetry_free_gb }}
      # ATENCIÓN: el nombre NO puede ser KERNEL_TOOLCHAIN (ver los20-build-stages.yml).
      AB_KERNEL_TC: ${{ github.event.inputs.kernel_toolchain }}

    steps:

      # ============================================================
      # RUNNER
      # ============================================================

      - name: Checkout infra repo (manifests)
        uses: actions/checkout@v4

      - name: Liberar espacio en disco
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false

      - name: Limpieza controlada de Swift preinstalado
        run: |
          set -e
          echo "=== SWIFT PRE-INSTALL ANTES ==="
          df -h /
          if [ ! -d /usr/share/swift ]; then
            echo "skip: /usr/share/swift no existe -> nada que limpiar"
            exit 0
          fi
          du -sh /usr/share/swift
          sudo rm -rf -- /usr/share/swift
          if [ -e /usr/share/swift ]; then
            echo "::error::no se pudo eliminar /usr/share/swift"
            exit 1
          fi
          echo "OK: /usr/share/swift eliminado"
          echo "=== TOOLCHAINS ADICIONALES (miniconda/pipx/az/microsoft) ==="
          for T in /usr/share/miniconda /opt/pipx /usr/share/az_15.6.1 /opt/microsoft; do
            if [ ! -e "$T" ]; then
              echo "skip: $T no existe -> nada que limpiar"
              continue
            fi
            du -sh "$T"
            sudo rm -rf -- "$T"
            if [ -e "$T" ]; then
              echo "::error::no se pudo eliminar $T"
              exit 1
            fi
            echo "OK: $T eliminado"
          done
          echo "=== SWIFT PRE-INSTALL DESPUÉS ==="
          df -h /

      - name: Limpieza controlada off-out (kotlinc/gradle/runner-2.336.0/actions no usadas)
        run: |
          set -e
          echo "=== OFF-OUT EXTRA ANTES ==="
          df -h /
          # Candidatos verificados como seguros (auditoría read-only):
          # kotlinc 94M (build usa external/kotlinc), gradle 165M (sin uso),
          # archivos 2.336.0 (agente en uso es 2.337.0, updates esperan fin
          # del job), paquetes de actions no usadas por este workflow.
          for T in \
            /usr/share/kotlinc \
            /usr/share/gradle-9.7.1 \
            /opt/runner-cache/actions-runner-linux-x64-2.336.0.tar.gz \
            /opt/runner-cache/actions-runner-linux-x64-2.336.0.tar.zst \
            /opt/actionarchivecache/actions_setup-node \
            /opt/actionarchivecache/actions_setup-python \
            /opt/actionarchivecache/github_codeql-action \
            /opt/actionarchivecache/github_gh-aw-actions; do
            # Allowlist estática: SOLO estas 8 rutas exactas pueden borrarse.
            # (No se verifica existencia de rutas protegidas aquí porque en
            # esta fase los20 aún no existe: lo crea repo init después.
            # Run 34792065347 demostró que exigir su existencia aborta el job.)
            case "$T" in
              /usr/share/kotlinc|/usr/share/gradle-9.7.1|/opt/runner-cache/actions-runner-linux-x64-2.336.0.tar.gz|/opt/runner-cache/actions-runner-linux-x64-2.336.0.tar.zst|/opt/actionarchivecache/actions_setup-node|/opt/actionarchivecache/actions_setup-python|/opt/actionarchivecache/github_codeql-action|/opt/actionarchivecache/github_gh-aw-actions)
                ;;
              *)
                echo "::error::ruta no autorizada: $T"
                exit 1
                ;;
            esac
            if [ ! -e "$T" ]; then
              echo "skip: $T no existe -> nada que limpiar"
              continue
            fi
            du -sh "$T"
            sudo rm -rf -- "$T"
            if [ -e "$T" ]; then
              echo "::error::no se pudo eliminar $T"
              exit 1
            fi
            echo "OK: $T eliminado"
          done
          echo "=== OFF-OUT EXTRA DESPUÉS ==="
          df -h /

      - name: Disco antes
        run: df -h /

      # ============================================================
      # Storage diag (traído de build-stages)
      # ============================================================

      - name: Diagnosticar almacenamiento
        run: |
          echo "=== DF ==="
          df -h

          echo
          echo "=== LSBLK ==="
          lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINTS

          echo
          echo "=== FINDMNT ==="
          findmnt

          echo
          echo "=== /mnt ==="
          ls -lah /mnt 2>/dev/null || true
          df -h /mnt 2>/dev/null || true

      # ============================================================
      # DEPENDENCIAS (ccache eliminado: ver env)
      # ============================================================

      - name: Dependencias e instalación de repo
        run: |
          set -e
          sudo apt-get update
          sudo apt-get install -y git-lfs python3 curl
          mkdir -p "$HOME/bin"
          curl -s https://storage.googleapis.com/git-repo-downloads/repo > "$HOME/bin/repo"
          chmod a+x "$HOME/bin/repo"
          echo "$HOME/bin" >> "$GITHUB_PATH"
          git config --global user.name  "j2lte-ci"
          git config --global user.email "j2lte-ci@users.noreply.github.com"
          git lfs install

          # symlinks de compat (traídos de build-stages)
          sudo ln -sf \
            /usr/lib/x86_64-linux-gnu/libncurses.so.6 \
            /usr/lib/x86_64-linux-gnu/libncurses.so.5 || true

          sudo ln -sf \
            /usr/lib/x86_64-linux-gnu/libtinfo.so.6 \
            /usr/lib/x86_64-linux-gnu/libtinfo.so.5 || true

          sudo apt-get install -y libc++1 >/dev/null 2>&1 || \
            sudo ln -sf \
              /usr/lib/x86_64-linux-gnu/libc++.so.1 \
              /usr/lib/x86_64-linux-gnu/libc++.so || true

      # ============================================================
      # SOURCE
      # ============================================================

      - name: repo init LineageOS 20.0
        working-directory: ${{ github.workspace }}
        run: |
          set -e
          mkdir -p "$HOME/los20"
          cd "$HOME/los20"
          repo init \
            --depth=1 \
            -u https://github.com/LineageOS/android.git \
            -b lineage-20.0 \
            --git-lfs

      - name: "local_manifests: j2lte + SLSI deps"
        working-directory: ${{ github.workspace }}
        run: |
          set -e
          mkdir -p .repo/local_manifests 2>/dev/null || true
          mkdir -p "$HOME/los20/.repo/local_manifests"
          cp "$GITHUB_WORKSPACE/manifests/j2lte.xml"      "$HOME/los20/.repo/local_manifests/j2lte.xml"
          cp "$GITHUB_WORKSPACE/manifests/j2lte-slsi.xml" "$HOME/los20/.repo/local_manifests/j2lte-slsi.xml"
          if [ -f "$GITHUB_WORKSPACE/manifests/j2lte-slim.xml" ]; then
            cp "$GITHUB_WORKSPACE/manifests/j2lte-slim.xml" "$HOME/los20/.repo/local_manifests/j2lte-slim.xml"
          fi
          cat "$HOME/los20/.repo/local_manifests/"*.xml

      - name: repo sync (-c --no-tags)
        working-directory: ${{ github.workspace }}
        run: |
          set -e
          cd "$HOME/los20"
          echo "=== DISCO PRE-SYNC ==="
          df -h /
          time repo sync -c --no-tags --fail-fast -j4
          echo "=== DISCO POST-SYNC ==="
          df -h /

      # ============================================================
      # PODAS POST-SYNC (sincronizadas desde los20-build-stages.yml)
      # ============================================================

      - name: Poda post-sync (prebuilts/rust/darwin-x86)
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA RUST DARWIN ANTES ==="
          df -h /
          du -sh prebuilts/rust/darwin-x86 2>/dev/null || true

          # Verificaciones de seguridad: debe existir como directorio
          if [ ! -d "prebuilts/rust/darwin-x86" ]; then
            echo "::error::Directorio esperado no existe: prebuilts/rust/darwin-x86 -> se aborta la poda"
            exit 1
          fi

          # Verificar que resuelva exactamente dentro del árbol (no un enlace ambiguo)
          PARENT="$(cd prebuilts/rust/darwin-x86 && pwd)"
          EXPECTED="$(pwd)/prebuilts/rust/darwin-x86"
          if [ "$PARENT" != "$EXPECTED" ]; then
            echo "::error::Resolución inesperada para prebuilts/rust/darwin-x86 ($PARENT != $EXPECTED) -> abort"
            exit 1
          fi

          # Verificar que la toolchain linux-x86 (única usada en runner Linux) SÍ existe
          if [ ! -d "prebuilts/rust/linux-x86" ]; then
            echo "::error::prebuilts/rust/linux-x86 no existe -> se aborta (se protege la toolchain activa)"
            exit 1
          fi

          rm -rf prebuilts/rust/darwin-x86

          echo "=== PODA RUST DARWIN DESPUÉS ==="
          df -h /
          ls -d prebuilts/rust/darwin-x86 2>/dev/null \
            && echo "::error::la poda no eliminó por completo" \
            || echo "OK: prebuilts/rust/darwin-x86 eliminado"

      - name: Poda post-sync (prebuilts/rust/linux-x86 1.57.0/1.58.0/1.58.1)
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA RUST VERSIONES NO ACTIVAS ANTES ==="
          df -h /
          VERSIONS="prebuilts/rust/linux-x86/1.57.0 prebuilts/rust/linux-x86/1.58.0 prebuilts/rust/linux-x86/1.58.1"
          du -sh $VERSIONS 2>/dev/null || true

          # Verificar que la toolchain activa (RustDefaultVersion = 1.59.0) SÍ existe
          if [ ! -d "prebuilts/rust/linux-x86/1.59.0" ]; then
            echo "::error::prebuilts/rust/linux-x86/1.59.0 no existe -> se aborta (se protege la toolchain activa)"
            exit 1
          fi

          for V in $VERSIONS; do
            # Verificaciones de seguridad: debe existir como directorio
            if [ ! -d "$V" ]; then
              echo "::error::Directorio esperado no existe: $V -> se aborta la poda"
              exit 1
            fi

            # Verificar que resuelva exactamente dentro del árbol (no un enlace ambiguo)
            PARENT="$(cd "$V" && pwd)"
            EXPECTED="$(pwd)/$V"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $V ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$V"

            ls -d "$V" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $V" \
              || echo "OK: $V eliminado"
          done

          echo "=== PODA RUST VERSIONES NO ACTIVAS DESPUÉS ==="
          df -h /

      - name: Poda post-sync (prebuilts/module_sdk/art snapshot inactivo @3)
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA MODULE_SDK ART/3 ANTES ==="
          df -h /
          TARGET=prebuilts/module_sdk/art/3
          du -sh "$TARGET" 2>/dev/null || true

          # Verificar que el snapshot activo (@1) del proyecto module_sdk/art SÍ existe
          if [ ! -d "prebuilts/module_sdk/art/1" ]; then
            echo "::error::prebuilts/module_sdk/art/1 no existe -> se aborta (se protege el snapshot activo @1)"
            exit 1
          fi

          # Verificaciones de seguridad: debe existir como directorio
          if [ ! -d "$TARGET" ]; then
            echo "::error::Directorio esperado no existe: $TARGET -> se aborta la poda"
            exit 1
          fi

          # Verificar que resuelva exactamente dentro del árbol (no un enlace ambiguo)
          PARENT="$(cd "$TARGET" && pwd)"
          EXPECTED="$(pwd)/$TARGET"
          if [ "$PARENT" != "$EXPECTED" ]; then
            echo "::error::Resolución inesperada para $TARGET ($PARENT != $EXPECTED) -> abort"
            exit 1
          fi

          rm -rf "$TARGET"

          echo "=== PODA MODULE_SDK ART/3 DESPUÉS ==="
          df -h /
          ls -d "$TARGET" 2>/dev/null \
            && echo "::error::la poda no eliminó por completo" \
            || echo "OK: $TARGET eliminado"

      - name: Poda post-sync (prebuilts/android-emulator + remoteexecution-client)
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA ANDROID-EMULATOR + REMOTEEXECUTION ANTES ==="
          df -h /
          du -sh prebuilts/android-emulator prebuilts/remoteexecution-client 2>/dev/null || true

          # Tolerante: el manifest j2lte-slim.xml ya puede haberlos excluido del
          # repo sync via <remove-project>. Si no existen, se informa y se
          # continúa (no abortar). Si existen, se podan.
          MISSING=0
          for d in android-emulator remoteexecution-client; do
            if [ ! -d "prebuilts/$d" ]; then
              echo "skip: prebuilts/$d no existe (ya excluido por j2lte-slim.xml en repo sync) -> nada que podar"
              MISSING=1
              continue
            fi
            PARENT="$(cd prebuilts/$d && pwd)"
            EXPECTED="$(pwd)/prebuilts/$d"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada prebuilts/$d ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi
          done

          if [ "$MISSING" = "1" ]; then
            echo "OK: sin poda post-sync necesaria (usa j2lte-slim.xml) -> continue"
          else
            rm -rf prebuilts/android-emulator prebuilts/remoteexecution-client
          fi

          echo "=== PODA ANDROID-EMULATOR + REMOTEEXECUTION DESPUÉS ==="
          df -h /
          for d in android-emulator remoteexecution-client; do
            ls -d prebuilts/$d 2>/dev/null \
              && echo "::error::la poda no eliminó $d" \
              || echo "OK: prebuilts/$d eliminado"
          done

      - name: Poda post-sync (prebuilts/runtime/mainline)
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA RUNTIME/MAINLINE ANTES ==="
          df -h /

          # Tolerante: si no existe (posible exclusión vía manifest), se informa y continúa.
          if [ ! -d "prebuilts/runtime/mainline" ]; then
            echo "skip: prebuilts/runtime/mainline no existe -> nada que podar"
            exit 0
          fi

          # Verificar que resuelva exactamente dentro del árbol (no un enlace ambiguo)
          PARENT="$(cd prebuilts/runtime/mainline && pwd)"
          EXPECTED="$(pwd)/prebuilts/runtime/mainline"
          if [ "$PARENT" != "$EXPECTED" ]; then
            echo "::error::Resolución inesperada para prebuilts/runtime/mainline ($PARENT != $EXPECTED) -> abort"
            exit 1
          fi

          rm -rf prebuilts/runtime/mainline

          echo "=== PODA RUNTIME/MAINLINE DESPUÉS ==="
          df -h /
          ls -d prebuilts/runtime/mainline 2>/dev/null \
            && echo "::error::la poda no eliminó por completo" \
            || echo "OK: prebuilts/runtime/mainline eliminado"

      - name: Poda post-sync (Ronda 2 6 candidatas test/docs/clang)
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA RONDA 2 ANTES ==="
          df -h /
          TARGETS="
            tools/dexter/testdata
            external/libabigail/tests
            prebuilts/clang/host/linux-x86/clang-stable
            prebuilts/clang/host/linux-x86/profiles
          "
          du -sh $TARGETS 2>/dev/null || true

          # Tolerante: si la ruta no existe se informa y se continúa (no abortar).
          # Protección: nunca eliminar un proyecto Git completo (contiene .git),
          # porque rompería repo manifest (patrón jdk/qemu documentado).
          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git completo, no subdir podable) -> se aborta"
              exit 1
            fi

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA RONDA 2 DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (Tier 1: 9 candidatas SAFE)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA TIER 1 (9 SAFE) ANTES ==="
          df -h /
          TARGETS="
            external/jackson-databind/docs
            external/swiftshader/tests
            prebuilts/clang-tools/darwin-x86
            prebuilts/build-tools/linux_musl-x86
            external/tensorflow/tensorflow/compiler
            external/tensorflow/tensorflow/python
            external/tensorflow/tensorflow/lite/testing
            external/deqp/external/openglcts
            prebuilts/build-tools/darwin-x86
          "
          du -sh $TARGETS 2>/dev/null || true

          # Tolerante: si la ruta no existe se informa y se continúa (no abortar).
          # Protección: nunca eliminar un proyecto Git completo (contiene .git),
          # porque rompería repo manifest (patrón jdk/qemu documentado).
          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git completo, no subdir podable) -> se aborta"
              exit 1
            fi

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA TIER 1 DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (autotest client+server)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA AUTOTEST ANTES ==="
          df -h /
          TARGETS="
            external/autotest/client
            external/autotest/server
          "
          du -sh $TARGETS 2>/dev/null || true

          # Tolerante: si la ruta no existe se informa y se continúa (no abortar).
          # Protección: nunca eliminar un proyecto Git completo (contiene .git),
          # porque rompería repo manifest (patrón jdk/qemu documentado).
          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git completo, no subdir podable) -> se aborta"
              exit 1
            fi

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA AUTOTEST DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (sdk/32)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA SDK/32 ANTES ==="
          df -h /
          TARGETS="
            prebuilts/sdk/32
          "
          du -sh $TARGETS 2>/dev/null || true

          # Tolerante: si la ruta no existe se informa y se continúa (no abortar).
          # Protección: nunca eliminar un proyecto Git completo (contiene .git),
          # porque rompería repo manifest (patrón jdk/qemu documentado).
          # prebuilts/sdk/32 es subdir sin .git (el proyecto es prebuilts/sdk);
          # cero sdk_version:"32" productivos en el árbol.
          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git completo, no subdir podable) -> se aborta"
              exit 1
            fi

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA SDK/32 DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (extras profiling)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA EXTRAS ANTES ==="
          df -h /
          TARGETS="
            system/extras/ioshark
          "
          du -sh $TARGETS 2>/dev/null || true

          # Tolerante: si la ruta no existe se informa y se continúa (no abortar).
          # Protección: nunca eliminar un proyecto Git completo (contiene .git),
          # porque rompería repo manifest (patrón jdk/qemu documentado).
          # Herramientas de profiling host sin consumidores en el grafo j2lte
          # (verificado: sin refs en system/frameworks/packages/cts/vendor).
          # NOTA: system/extras/simpleperf NO se poda (dependencias de
          # perf2cfg, profcollectd y cts-simpleperf en sus módulos).
          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git completo, no subdir podable) -> se aborta"
              exit 1
            fi

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA EXTRAS DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (Darwin arch remnants)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA DARWIN ANTES ==="
          df -h /
          TARGETS="
            prebuilts/jdk/jdk11/darwin-x86
            prebuilts/jdk/jdk11/darwin-arm64
            prebuilts/jdk/jdk8/darwin-x86
            prebuilts/build-tools/darwin-x86
            prebuilts/extract-tools/darwin-x86
            prebuilts/misc/darwin-x86
            prebuilts/misc/darwin-x86_64
          "
          du -sh $TARGETS 2>/dev/null || true

          # Binarios macOS inalcanzables en runner linux-x86_64: el build
          # selecciona prebuilts por HOST_OS/HOST_TAG (Soong HostPrebuiltTag
          # retorna "linux-x86" si runtime.GOOS=="linux"; make con ramas
          # ifeq ($(HOST_OS),darwin)). Barrido global sin referencias.
          # jdk11-linux, jdk8-linux, build-tools-linux, extract-linux y
          # misc-linux quedan INTACTOS (solo se listan darwin-*).
          # Protección: nunca eliminar un proyecto Git completo (contiene
          # .git); estos 7 son subdirs sin .git (padres con .git).
          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git completo, no subdir podable) -> se aborta"
              exit 1
            fi

            case "$T" in
              prebuilts/jdk/jdk11/darwin-*|prebuilts/jdk/jdk8/darwin-*|prebuilts/build-tools/darwin-*|prebuilts/extract-tools/darwin-*|prebuilts/misc/darwin-*) ;;
              *) echo "::error::$T fuera de la allowlist Darwin -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA DARWIN DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (sdk/tools lib)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA SDK-TOOLS-LIB ANTES ==="
          df -h /
          # SOLO lib auditado (d8/dx/signapk prebuilt sin refs; firma→fuente,
          # dex→prebuilts/r8). linux RESTAURADO (run #155: el genrule
          # build-tools-lld-linux del propio sdk/tools/Android.bp:77 copia
          # linux/lld-bin/lld y linux/lib64/libc++.so.1 por path relativo).
          # NO tocar sdk/tools/darwin, windows (prohibidos), niveles
          # 28-33/current, jetifier (futura tanda).
          TARGETS="
            prebuilts/sdk/tools/lib
          "
          du -sh $TARGETS 2>/dev/null || true

          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git, no subdir) -> se aborta"
              exit 1
            fi

            case "$T" in
              prebuilts/sdk/tools/lib) ;;
              *) echo "::error::$T fuera de la allowlist sdk-lib -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            # Proteger el padre platform/prebuilts/sdk y .repo: solo rm del subdir.
            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA SDK-TOOLS-LIB DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (tools/common/metalava)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA METALAVA ANTES ==="
          df -h /
          # SOLO metalava prebuilt auditado (sin .bp, cero refs; metalava se
          # compila de fuente). NO tocar resto de tools/common ni .repo.
          TARGETS="
            prebuilts/tools/common/metalava
          "
          du -sh $TARGETS 2>/dev/null || true

          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git, no subdir) -> se aborta"
              exit 1
            fi

            case "$T" in
              prebuilts/tools/common/metalava) ;;
              *) echo "::error::$T fuera de la allowlist metalava -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA METALAVA DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (runtime/appcompat)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA APPCOMPAT ANTES ==="
          df -h /
          # SOLO appcompat auditado (appcompat.sh/veridex dev-tool externo;
          # cero refs en build/art/frameworks). NO tocar resto de runtime
          # (mainline ya podado; adb/iorap/appcompat-activo fuera de alcance).
          TARGETS="
            prebuilts/runtime/appcompat
          "
          du -sh $TARGETS 2>/dev/null || true

          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git, no subdir) -> se aborta"
              exit 1
            fi

            case "$T" in
              prebuilts/runtime/appcompat) ;;
              *) echo "::error::$T fuera de la allowlist appcompat -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA APPCOMPAT DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (chicas auditadas R1-F)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA CHICAS ANTES ==="
          df -h /
          # Subdirs auditados con grep -F + control positivo, cero
          # consumidores (bionic-bench/apex/brotli) o samples sin cierre
          # (device/sample/apps; etc/ INTACTO por apns-full-conf.xml).
          # gcam EXCLUIDA (inventario incompleto). ahat RESTAURADO (run #159).
          # bionic/tests RESTAURADO (run #162: CtsBionicTestCases necesita
          # libBionicTests y libs hermanas de bionic/tests; inventario previo
          # solo cubrió bionic-unit-tests*, no las test-libs).
          # apex/tests RESTAURADO (run #165: ApexTestCases en apexd necesita
          # sharedlibs_generated de tests/testdata; inventario no bajó ahí).
          # jsr166-tests RESTAURADO (run #166: módulo `jsr166-tests` definido
          # en libcore/JavaLibrary.bp:545 con srcs DENTRO del dir podado ->
          # Kati "Installable device module must have LOCAL_SOONG_DEX_JAR".
          # Trampa inversa: módulo fuera, fuentes dentro. harmony/benchmarks
          # siguen podados (sin errores en #166; si fallan, se restauran).
          # bionic/benchmarks RESTAURADO (run #167:
          # native_metric_test_list.mk lo exige; R5 miraba solo
          # native_test_list.mk, no las 5 listas).
          TARGETS="
            prebuilts/tools/common/google-api-java-client
            libcore/harmony-tests
            libcore/benchmarks
            external/brotli/tests/testdata
            device/sample/apps
          "
          du -sh $TARGETS 2>/dev/null || true

          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git, no subdir) -> se aborta"
              exit 1
            fi

            case "$T" in
              prebuilts/tools/common/google-api-java-client|libcore/harmony-tests|libcore/benchmarks|external/brotli/tests/testdata|device/sample/apps) ;;
              *) echo "::error::$T fuera de la allowlist chicas-R1F -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA CHICAS DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (misc-frameworks)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA MISCFW ANTES ==="
          df -h /
          # SOLO platformprotos prebuilt (cero consumidores globales,
          # libplatformprotos real se compila de frameworks/base).
          TARGETS="
            prebuilts/misc/common/frameworks
          "
          du -sh $TARGETS 2>/dev/null || true

          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git, no subdir) -> se aborta"
              exit 1
            fi

            case "$T" in
              prebuilts/misc/common/frameworks) ;;
              *) echo "::error::$T fuera de la allowlist miscfw -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA MISCFW DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (webview no-arm)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA WEBVIEW-NOARM ANTES ==="
          df -h /
          # SOLO los 3 APKs no-arm auditados (x86_64/arm64/x86). j2lte resuelve
          # prebuilt/$(TARGET_ARCH=arm) vía get-prebuilt-src-arch; cero refs
          # a los otros en device/vendor/build/frameworks/packages/system/
          # hardware/external/tools/art/prebuilts (barridos completados, sin
          # globs: Android.mk usa ruta explícita por arch). arm/ INTACTO.
          TARGETS="
            external/chromium-webview/prebuilt/x86_64/webview.apk
            external/chromium-webview/prebuilt/arm64/webview.apk
            external/chromium-webview/prebuilt/x86/webview.apk
          "
          du -sh $TARGETS 2>/dev/null || true

          for T in $TARGETS; do
            if [ ! -f "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            case "$T" in
              external/chromium-webview/prebuilt/x86_64/webview.apk|external/chromium-webview/prebuilt/arm64/webview.apk|external/chromium-webview/prebuilt/x86/webview.apk) ;;
              *) echo "::error::$T fuera de la allowlist webview-noarm -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$(dirname "$T")" && pwd)"
            EXPECTED="$(pwd)/external/chromium-webview/prebuilt"
            case "$PARENT" in
              "$EXPECTED"/x86_64|"$EXPECTED"/arm64|"$EXPECTED"/x86) ;;
              *) echo "::error::ruta inesperada para $T -> abort"; exit 1 ;;
            esac

            rm -f "$T"

            ls "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA WEBVIEW-NOARM DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (contents+sysroots)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA CONTENTS/SYSROOTS ANTES ==="
          df -h /
          # contents: test media sin refs globales (todo tipo); sysroots:
          # musl cuyo toolchain ya fue podado. Padres intactos.
          TARGETS="
            frameworks/base/media/tests/contents
            prebuilts/build-tools/sysroots
          "
          du -sh $TARGETS 2>/dev/null || true

          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git, no subdir) -> se aborta"
              exit 1
            fi

            case "$T" in
              frameworks/base/media/tests/contents|prebuilts/build-tools/sysroots) ;;
              *) echo "::error::$T fuera de la allowlist -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA CONTENTS/SYSROOTS DESPUÉS ==="
          df -h /

      - name: "Poda post-sync (rs-prebuilt+siblings+clangpy+incidentd+svctests)"
        working-directory: /home/runner/los20
        run: |
          set -e
          echo "=== PODA TANDA2 ANTES ==="
          df -h /
          # samsung/j1xlte+on5ltetmo (blobs hermanas, guards por device),
          # clang/python3 (bundled lldb, python sistema en uso),
          # incidentd/testdata (cero refs). servicestests RESTAURADO
          # (run #174). renderscript RESTAURADO (run #175:
          # development/build `build-tools` necesita
          # `renderscript_sdk_prebuilts`; reversa no cubrió development/).
          TARGETS="
            vendor/samsung/j1xlte
            vendor/samsung/on5ltetmo
            prebuilts/clang/host/linux-x86/clang-r450784d/python3
            frameworks/base/cmds/incidentd/testdata
          "
          du -sh $TARGETS 2>/dev/null || true

          for T in $TARGETS; do
            if [ ! -d "$T" ]; then
              echo "skip: $T no existe -> nada que podar"
              continue
            fi

            if [ -e "$T/.git" ]; then
              echo "::error::$T contiene .git (proyecto Git, no subdir) -> se aborta"
              exit 1
            fi

            case "$T" in
              vendor/samsung/j1xlte|vendor/samsung/on5ltetmo|prebuilts/clang/host/linux-x86/clang-r450784d/python3|frameworks/base/cmds/incidentd/testdata) ;;
              *) echo "::error::$T fuera de la allowlist tanda2 -> se aborta"; exit 1 ;;
            esac

            PARENT="$(cd "$T" && pwd)"
            EXPECTED="$(pwd)/$T"
            if [ "$PARENT" != "$EXPECTED" ]; then
              echo "::error::Resolución inesperada para $T ($PARENT != $EXPECTED) -> abort"
              exit 1
            fi

            rm -rf "$T"

            ls -d "$T" 2>/dev/null \
              && echo "::error::la poda no eliminó por completo $T" \
              || echo "OK: $T eliminado"
          done

          echo "=== PODA TANDA2 DESPUÉS ==="
          df -h /

      - name: "Filtro .repo opción A (blobs) + guards"
        working-directory: /home/runner/los20
        run: |
          set -e
          # OPCIÓN A validada en /tmp/repo-test (prueba reversible 2026-09-21):
          # conserva commits+tags+trees, elimina SOLO blobs. Mantiene
          # `repo manifest -r` y setlocalversion (rev-parse + diff-index).
          # NO elimina .repo, NO usa manifest slim, NO toca device/kernel/vendor.
          echo "=== FILTRO-A .repo ANTES ==="
          df -h /
          du -sh .repo 2>/dev/null || true
          du -sh .repo/project-objects 2>/dev/null || true
          du -sh .repo/projects 2>/dev/null || true
          FREE_BEFORE_MB=$(df -B1 / | awk 'NR==2{print int($4/1048576)}')
          echo "FREE_BEFORE_MB=$FREE_BEFORE_MB"

          # G1: manifest baseline (si falla aquí, el sync ya está roto -> abort).
          python3 .repo/repo/repo manifest -o /tmp/manifest-before.xml -r
          test -s /tmp/manifest-before.xml || { echo "::error::manifest-before vacío -> abort"; exit 1; }
          echo "OK: manifest-before capturado"

          # G2: todos los HEAD resuelven ANTES (baseline del sync).
          UNRES_BEFORE=$(repo forall -c 'git rev-parse --verify -q HEAD >/dev/null 2>&1 || echo "UNRESOLVED:$REPO_PATH"' 2>/dev/null || true)
          if [ -n "$UNRES_BEFORE" ]; then
            echo "::error::HEADs sin resolver ANTES del filtrado -> abort (sync incompleto):"
            echo "$UNRES_BEFORE" | head -10
            exit 1
          fi
          echo "OK: todos los HEAD resuelven antes del filtrado"

          # --- driver validado: script temporal + xargs -P4 (mismo patrón
          # que la prueba /tmp/repo-test 2515/2515; sin export -f) ---
          ROOT="$(pwd)"
          case "$ROOT" in
            */los20) ;;
            *) echo "::error::cwd inesperado ($ROOT), se esperaba .../los20 -> abort"; exit 1 ;;
          esac
          test -d .repo -a -f build/envsetup.sh || { echo "::error::no es checkout los20 -> abort"; exit 1; }
          cat > /tmp/filter-one-ci.sh <<'FILTER_EOF'
          #!/bin/bash
          # Filtra UN git dir: conserva commits+tags+trees, elimina solo blobs.
          # Lógica idéntica a filter-one.sh validado en /tmp/repo-test.
          # Uso: filter-one-ci.sh <gitdir-relativo-al-checkout>
          g="$1"
          LOG=/tmp/filter-a.log
          case "$g" in
            .repo/*) ;;
            *) echo "RECHAZADO (fuera de .repo): $g" >>"$LOG"; exit 2 ;;
          esac
          case "$g" in
            .repo/repo*|.repo/manifests*) echo "SKIP(tool) $g" >>"$LOG"; exit 0 ;;
          esac
          [ -d "$g/objects" ] || { echo "SKIP(no-objects) $g" >>"$LOG"; exit 0; }
          # Los stores de CI pueden no traer objects/pack/ (objetos loose);
          # se crea (a través del symlink comparte el store real; idempotente).
          mkdir -p "$g/objects/pack" 2>/dev/null \
            || { echo "FAIL(mkdir) $g" >>"$LOG"; exit 1; }
          keep="$(mktemp)"
          git -C "$g" cat-file --batch-check --batch-all-objects 2>/dev/null \
            | awk '$2=="commit"||$2=="tree"||$2=="tag"{print $1}' > "$keep"
          n=$(wc -l < "$keep")
          if [ "$n" -eq 0 ]; then echo "SKIP(empty) $g" >>"$LOG"; rm -f "$keep"; exit 0; fi
          if ! git -C "$g" pack-objects --depth=0 --non-empty "$PWD/$g/objects/pack/keep-pack" < "$keep" >>"$LOG" 2>&1; then
            echo "FAIL(pack) $g" >>"$LOG"; rm -f "$keep"; exit 1
          fi
          rm -f "$keep"
          newidx=$(ls "$g"/objects/pack/keep-pack-*.idx 2>/dev/null | head -1)
          [ -n "$newidx" ] || { echo "FAIL(no-newidx) $g" >>"$LOG"; exit 1; }
          nb=$(git verify-pack -v "$newidx" 2>/dev/null | awk '$2=="blob"' | wc -l)
          [ "$nb" -eq 0 ] || { echo "FAIL(blobs=$nb) $g" >>"$LOG"; rm -f "$g"/objects/pack/keep-pack-*; exit 1; }
          if ! git -C "$g" rev-parse --all >/dev/null 2>&1; then
            echo "FAIL(refs) $g" >>"$LOG"; rm -f "$g"/objects/pack/keep-pack-*; exit 1
          fi
          # Recién ahora: borrar packs viejos + loose blobs (nunca trees/commits/tags).
          find "$g/objects/pack" \( -name 'pack-*.pack' -o -name 'pack-*.idx' -o -name 'pack-*.rev' -o -name 'pack-*.bitmap' -o -name 'pack-*.promisor' \) ! -name 'keep-pack-*' -delete
          # Borrado batcheado de loose-blobs (una sola invocación git;
          # conjunto idéntico al loop anterior: solo paths loose cuyo objeto es blob).
          git -C "$g" cat-file --batch-check --batch-all-objects 2>/dev/null \
            | awk '$2=="blob" {print substr($1,1,2)"/"substr($1,3)}' \
            | while read -r p; do rm -f "$g/objects/$p"; done
          echo "OK $g" >>"$LOG"
          exit 0
          FILTER_EOF
          : > /tmp/filter-a.log
          : > /tmp/filter-a.err
          # Dedup por store real: mirrors (.repo/projects) y stores
          # (.repo/project-objects) comparten objects/ por symlink; se
          # procesa cada store UNA vez (prefiere ruta project-objects).
          find .repo -name "*.git" -type d | while read -r _g; do
            [ -d "$_g/objects" ] || continue
            _r=$(realpath "$_g/objects" 2>/dev/null) || continue
            echo "$_r $_g"
          done | sort -k1,1 -k2,2 | awk '!seen[$1]++ {print $2}' \
            | xargs -P 4 -I{} bash /tmp/filter-one-ci.sh "{}" 2>>/tmp/filter-a.err || XARGS_EXIT=$?
          XARGS_EXIT=${XARGS_EXIT:-0}
          echo "XARGS_EXIT=$XARGS_EXIT"
          echo "--- filter-a.log (primeras 20 líneas):"
          head -20 /tmp/filter-a.log
          echo "--- child stderr (primeras 20 líneas):"
          head -20 /tmp/filter-a.err
          echo "filtrados OK: $(grep -c '^OK' /tmp/filter-a.log || true) FAIL: $(grep -c '^FAIL' /tmp/filter-a.log || true)"
          test "$XARGS_EXIT" = "0" \
            || { echo "::error::xargs exit=$XARGS_EXIT -> abort"; exit 1; }
          test "$(grep -c '^FAIL' /tmp/filter-a.log || true)" = "0" \
            || { echo "::error::hubo FAILs en filtrado -> abort"; exit 1; }

          # G3: manifest DESPUÉS debe funcionar y ser idéntico al baseline.
          python3 .repo/repo/repo manifest -o /tmp/manifest-after.xml -r \
            || { echo "::error::repo manifest -r falló DESPUÉS del filtrado -> abort"; exit 1; }
          diff -q /tmp/manifest-before.xml /tmp/manifest-after.xml \
            || { echo "::error::el manifest CAMBIÓ tras el filtrado -> abort"; exit 1; }
          echo "OK: manifest idéntico antes/después"

          # G4: todos los HEAD resuelven DESPUÉS (objetos requeridos presentes).
          UNRES_AFTER=$(repo forall -c 'git rev-parse --verify -q HEAD >/dev/null 2>&1 || echo "UNRESOLVED:$REPO_PATH"' 2>/dev/null || true)
          if [ -n "$UNRES_AFTER" ]; then
            echo "::error::HEADs sin resolver DESPUÉS del filtrado -> abort (objeto requerido ausente):"
            echo "$UNRES_AFTER" | head -10
            exit 1
          fi
          echo "OK: todos los HEAD resuelven después del filtrado"

          # G5: operaciones git de setlocalversion (kernel) siguen funcionando.
          KDIR="kernel/samsung/exynos3475"
          git -C "$KDIR" rev-parse --verify --short HEAD >/dev/null \
            || { echo "::error::kernel rev-parse HEAD falló -> abort"; exit 1; }
          git -C "$KDIR" diff-index --name-only HEAD >/dev/null \
            || { echo "::error::kernel diff-index HEAD falló -> abort"; exit 1; }
          echo "OK: setlocalversion (rev-parse + diff-index) funciona"

          echo "=== FILTRO-A .repo DESPUÉS ==="
          df -h /
          du -sh .repo 2>/dev/null || true
          du -sh .repo/project-objects 2>/dev/null || true
          du -sh .repo/projects 2>/dev/null || true
          FREE_AFTER_MB=$(df -B1 / | awk 'NR==2{print int($4/1048576)}')
          echo "FREE_BEFORE_MB=$FREE_BEFORE_MB FREE_AFTER_MB=$FREE_AFTER_MB RECUPERADO_MB=$((FREE_AFTER_MB - FREE_BEFORE_MB))"
          test -d .repo/projects \
            && echo "OK: .repo/projects conservado" \
            || { echo "::error::.repo/projects desapareció -> abort"; exit 1; }

      - name: Upload filter-a forense (solo si el filtro falló)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: filter-a-bacon-${{ github.run_id }}
          path: |
            /tmp/filter-a.log
            /tmp/filter-a.err
            /tmp/manifest-before.xml
            /tmp/manifest-after.xml
          if-no-files-found: warn
          retention-days: 3

      # ============================================================
      # GUARD (después de TODAS las podas, igual que en stages)
      # ============================================================

      - name: GUARD espacio post-sync (FIX-014)
        run: |
          set -e
          AVAIL_GB=$(df --output=avail -BG / | tail -1 | tr -dc '0-9')
          MIN=${{ github.event.inputs.min_free_gb || 70 }}
          echo "Libres post-sync: ${AVAIL_GB}G (mínimo requerido para bacon: ${MIN}G)"
          if [ "${AVAIL_GB}" -lt "${MIN}" ]; then
            echo "::error::INFRASTRUCTURE-BLOCKED (FIX-014): ${AVAIL_GB}G libres < ${MIN}G requeridos para out/ + margen. No se ejecuta bacon."
            exit 90
          fi
          MODE='${{ github.event.inputs.mode }}'
          if [ "$MODE" != "full-bacon" ]; then
            echo "mode=lunch-only -> guard superado, se continúa solo hasta lunch."
          fi

      # ============================================================
      # BUILD CONFIG
      # ============================================================

      - name: Toolchain del kernel (experimento A/B)
        working-directory: /home/runner/los20
        run: |
          set -e
          case "$AB_KERNEL_TC" in
            clang)
              echo "kernel_toolchain=clang (CI estandar, sin cambios)"
              ;;
            gcc)
              CFG=device/samsung/universal3475-common/BoardConfigCommon.mk
              test -f "$CFG" || { echo "::error::falta $CFG"; exit 2; }
              # Inyección EFÍMERA: solo en el checkout del runner; no commitea nada.
              printf '\n# [A/B experiment] kernel con GCC 4.9 (kernel.mk: CLANG_COMPILE=false -> CC=arm-linux-androidkernel-gcc, sin deps clang salvo HOSTCC)\nTARGET_KERNEL_CLANG_COMPILE := false\n' >> "$CFG"
              grep -n "TARGET_KERNEL_CLANG_COMPILE" "$CFG"
              # HOSTLDFLAGS/HOSTCFLAGS usan -fuse-ld=lld -> asegurar ld.lld en PATH.
              echo "/home/runner/los20/prebuilts/clang/host/linux-x86/clang-r450784d/bin" >> "$GITHUB_PATH"
              echo "kernel_toolchain=gcc -> TARGET_KERNEL_CLANG_COMPILE=false inyectado"
              ;;
            *)
              echo "::error::kernel_toolchain inválido: $AB_KERNEL_TC"
              exit 64
              ;;
          esac

      - name: source envsetup.sh + lunch lineage_j2lte-userdebug
        working-directory: /home/runner/los20
        run: |
          set -e
          source build/envsetup.sh
          lunch lineage_j2lte-userdebug
          echo "LUNCH_EXIT=$?"

      # ============================================================
      # BUILD
      # ============================================================

      - name: Medición de disco ANTES del build
        if: ${{ github.event.inputs.mode == 'full-bacon' }}
        working-directory: /home/runner/los20
        run: |
          set +e
          echo "=== DISCO ANTES DE mka bacon ==="
          df -h /
          du -sh out 2>/dev/null || true
          echo "=== Directorios más grandes en out/ ==="
          du -sh out/* 2>/dev/null | sort -h | tail -20 || true

      - name: Diagnóstico de almacenamiento fuera de out
        if: ${{ github.event.inputs.mode == 'full-bacon' }}
        working-directory: /home/runner/los20
        run: |
          set +e
          echo "===== STORAGE DIAGNOSTIC: BEFORE BACON ====="
          df -h /
          echo "===== TOP-LEVEL DIRECTORIES ====="
          du -sh /tmp /var/tmp /home/runner/.cache /home/runner/* 2>/dev/null | sort -h
          echo "===== SYSTEM DIRECTORIES ====="
          du -sh /opt/* /usr/share/* /var/log/* 2>/dev/null | sort -h | tail -30
          echo "===== END STORAGE DIAGNOSTIC ====="

      - name: Inspección read-only de runner-cache y Gradle
        if: ${{ github.event.inputs.mode == 'full-bacon' }}
        working-directory: /home/runner/los20
        run: |
          set +e
          echo "=== /opt/runner-cache ==="
          sudo du -sh /opt/runner-cache 2>/dev/null || true
          sudo ls -lah /opt/runner-cache 2>/dev/null || true
          sudo du -sh /opt/runner-cache/* 2>/dev/null | sort -h || true
          echo "=== /opt/actionarchivecache ==="
          sudo du -sh /opt/actionarchivecache 2>/dev/null || true
          sudo ls -lah /opt/actionarchivecache 2>/dev/null || true
          sudo du -sh /opt/actionarchivecache/* 2>/dev/null | sort -h || true
          echo "=== GRADLE EN PATH/WORKFLOW ==="
          command -v gradle || echo "gradle no está en PATH"
          ls -d /usr/share/gradle-9.7.1 2>/dev/null || echo "/usr/share/gradle-9.7.1 no existe"
          echo "=== KOTLINC (candidata registrada 94M, NO borrar) ==="
          du -sh /usr/share/kotlinc 2>/dev/null || true

      - name: mka bacon (build completo)
        if: ${{ github.event.inputs.mode == 'full-bacon' }}
        working-directory: /home/runner/los20
        run: |
          set -o pipefail

          # --- MONITOR DE DISCO (distinguir ENOSPC / kill externo / runner)
          #     traído de los20-build-stages.yml, adaptado a target único
          #     "bacon" (sin loop de $STAGE/$EXTRA). ---
          LOG=/tmp/disk-monitor.log
          : > "$LOG"
          TELEM_FREE_GB_MB=$(( (${TELEM_FREE_GB:-25}) * 1024 ))

          ninja_progress() {
            grep -aoE '\[ *[0-9]+% +[0-9]+/[0-9]+\]' /tmp/bacon.log 2>/dev/null \
              | tail -1 || true
          }

          dmesg_probe() {
            local out
            out=$(dmesg --ctime 2>/dev/null) || out="$(sudo -n dmesg --ctime 2>/dev/null)" || { echo "dmesg: no disponible"; return 0; }
            echo "$out" | grep -aiE 'out of memory|oom-kill|Killed process|hardware error|Call Trace|blk_update_request|I/O error' | tail -6
          }

          dmesg_tail() {
            local out
            out=$(dmesg --ctime 2>/dev/null) || out="$(sudo -n dmesg --ctime 2>/dev/null)" || { echo "dmesg: no disponible"; return 0; }
            echo "$out" | tail -40
          }

          state_cheap() {
            echo "ninja: $(ninja_progress)"
            df -h /
            df -i /
            echo "procs: clang=$(pgrep -c -f clang 2>/dev/null||echo 0) cc1plus=$(pgrep -c cc1plus 2>/dev/null||echo 0) ninja=$(pgrep -c ninja 2>/dev/null||echo 0)"
            grep -E 'Mem:|Swap:' /proc/meminfo
          }

          monitor_normal() {
            echo "=== $(date -u +%FT%T) [NORMAL 5min] ==="
            state_cheap
            du -sb -- out out/soong/.intermediates out/target/common/obj \
              out/target/product/j2lte/obj out/target/product/j2lte/symbols \
              2>/dev/null
            echo "--- top dirs out/ ---"
            du -sh out/* 2>/dev/null | sort -h | tail -20
            echo "--- archivos que crecieron en los últimos 5 min ---"
            find out -newermt '5 minutes ago' -type f -printf '%s %p\n' 2>/dev/null | sort -rn | head -15
          }

          monitor_dense() {
            echo "=== $(date -u +%FT%T) [DENSOS 5s] ==="
            state_cheap
            timeout 30 du -sh out 2>/dev/null
            timeout 30 du -sh out/soong 2>/dev/null
            timeout 30 du -sh out/host 2>/dev/null
            timeout 30 du -sh out/target 2>/dev/null
            timeout 30 du -sh out/soong/.intermediates/external/webrtc 2>/dev/null || true
            echo "--- dmesg ---"
            dmesg_probe
          }

          last_gasp() {
            echo "### BAJISMO free -> volcado final ###"
            df -h /
            df -i /
            free -m
            echo "ninja: $(ninja_progress)"
            ps -eo pid,ppid,stat,etime,args --sort=-%cpu | head -30
            pgrep -af 'clang|cc1plus|ninja|mka' || true
            echo "uptime: $(uptime)"
            dmesg_tail
          }

          (
            set +e
            while :; do
              free_mb=$(df -B1 / | awk 'NR==2{print $4/1048576}')
              if [ "${free_mb%.*}" -lt "$TELEM_FREE_GB_MB" ]; then
                echo "### UMBRAL ${TELEM_FREE_GB:-25}GB free=${free_mb}MB -> muestreo denso cada 5s ###" | tee -a "$LOG"
                while :; do
                  monitor_dense | tee -a "$LOG"
                  f2=$(df -B1 / | awk 'NR==2{print $4/1048576}')
                  if [ "${f2%.*}" -lt 100 ]; then
                    last_gasp | tee -a "$LOG" 2>&1
                    break
                  fi
                  sleep 5
                done
              else
                monitor_normal | tee -a "$LOG"
                sleep 300
              fi
            done
          ) &
          MON_PID=$!
          trap 'kill "$MON_PID" 2>/dev/null; wait "$MON_PID" 2>/dev/null || true; cat "$LOG" >&2' EXIT
          # --- FIN MONITOR ---

          export WITH_DEXPREOPT_BOOT_IMG_AND_SYSTEM_SERVER_ONLY=true

          echo "=== WITH_DEXPREOPT_BOOT_IMG_AND_SYSTEM_SERVER_ONLY=$WITH_DEXPREOPT_BOOT_IMG_AND_SYSTEM_SERVER_ONLY ==="

          source build/envsetup.sh
          lunch lineage_j2lte-userdebug >/dev/null

          echo "== mka bacon =="

          time mka --skip-soong-tests bacon -j"$(nproc)" 2>&1 | tee /tmp/bacon.log
          RC=${PIPESTATUS[0]}
          echo "BACON_RC=$RC"
          exit "$RC"

      - name: Rescate de telemetría ante fallo/ENOSPC/kill
        if: always()
        run: |
          set +e
          echo "=== RESCATE TELEMETRÍA ==="
          df -h / 2>&1
          df -i / 2>&1
          free -m 2>&1

          mkdir -p "$GITHUB_WORKSPACE/artifacts"

          for f in /tmp/bacon.log /tmp/disk-monitor.log; do
            if [ -f "$f" ]; then
              gzip -c "$f" > "$GITHUB_WORKSPACE/artifacts/$(basename "$f").gz" 2>/dev/null \
                || cp "$f" "$GITHUB_WORKSPACE/artifacts/" 2>/dev/null
            fi
          done

          ps -eo pid,ppid,stat,etime,comm,args --sort=-%mem | head -30 \
            > "$GITHUB_WORKSPACE/artifacts/ps-final.txt" 2>&1

          pgrep -af 'clang|cc1plus|ninja|mka' > "$GITHUB_WORKSPACE/artifacts/procs-final.txt" 2>&1 || true
          echo "uptime: $(uptime)" >> "$GITHUB_WORKSPACE/artifacts/procs-final.txt"

          if ! dmesg --ctime 2>/dev/null | tail -40 > "$GITHUB_WORKSPACE/artifacts/dmesg-final.txt"; then
            if ! sudo -n dmesg --ctime 2>/dev/null | tail -40 > "$GITHUB_WORKSPACE/artifacts/dmesg-final.txt"; then
              echo "dmesg: no disponible" > "$GITHUB_WORKSPACE/artifacts/dmesg-final.txt"
            fi
          fi

          echo "--- contenido de artifacts/ ---"
          ls -la "$GITHUB_WORKSPACE/artifacts/" 2>&1

      - name: Medición de disco DESPUÉS del build
        if: always()
        working-directory: /home/runner/los20
        run: |
          set +e
          echo "=== DISCO DESPUÉS DEL BUILD ==="
          df -h /
          du -sh out 2>/dev/null || true
          echo "=== Directorios más grandes en out/ ==="
          du -sh out/* 2>/dev/null | sort -h | tail -20 || true

      # ============================================================
      # RESULTADOS
      # ============================================================

      - name: Resumen y artefactos
        if: always()
        working-directory: /home/runner/los20
        run: |
          df -h /
          echo "=== out/target (si existe) ==="
          ls -la out/target/product/* 2>/dev/null || echo "(sin productos)"
          mkdir -p "$GITHUB_WORKSPACE/artifacts"
          if compgen -G "out/target/product/*/*.zip" > /dev/null; then
            cp out/target/product/*/*.zip "$GITHUB_WORKSPACE/artifacts/" || true
          fi
          cp /tmp/bacon.log "$GITHUB_WORKSPACE/artifacts/bacon.log" 2>/dev/null || true

      - name: Upload target files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: j2lte-target-files-${{ github.run_id }}
          path: artifacts/
          if-no-files-found: ignore

      - name: Copiar forense kernel (fuera del workspace -> dentro)
        if: ${{ always() && (github.event.inputs.mode == 'full-bacon' || github.event.inputs.kernel_toolchain == 'gcc') }}
        run: |
          set +e
          mkdir -p "$GITHUB_WORKSPACE/kernel-forensic"
          KOBJ=/home/runner/los20/out/target/product/j2lte/obj/KERNEL_OBJ
          cp "$KOBJ/.config" "$GITHUB_WORKSPACE/kernel-forensic/" 2>/dev/null || echo "skip: .config ausente"
          cp "$KOBJ/arch/arm/boot/zImage-dtb" "$GITHUB_WORKSPACE/kernel-forensic/" 2>/dev/null || echo "skip: zImage-dtb ausente"
          cp "$KOBJ"/arch/arm/boot/dts/exynos3475-j2lte_swa_open_*.dtb "$GITHUB_WORKSPACE/kernel-forensic/" 2>/dev/null || echo "skip: dtbs ausentes"
          ls -la "$GITHUB_WORKSPACE/kernel-forensic/"

      - name: Upload kernel forense (A/B toolchain)
        if: ${{ always() && (github.event.inputs.mode == 'full-bacon' || github.event.inputs.kernel_toolchain == 'gcc') }}
        uses: actions/upload-artifact@v4
        with:
          name: kernel-forensic-${{ github.event.inputs.kernel_toolchain }}-${{ github.run_id }}
          path: kernel-forensic/
          if-no-files-found: warn
